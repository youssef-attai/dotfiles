[module/filesystem]
type = internal/fs

mount-0 = /

fixed-values = true

label-mounted = %free%
format-mounted =  <label-mounted>
format-mounted-foreground = ${colors.sapphire}
format-mounted-padding = 1

interval = 1
